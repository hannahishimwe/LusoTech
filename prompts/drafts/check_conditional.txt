Examine the json below.
Take this route if and only if ALL of the following are true simultaneously:

1. All eight mandatory fields are present and non-null:
   * supplier_name
   * po_reference
   * heat_number
   * coil_lot_id
   * material_grade
   * manufacturing_date
   * certificate_type
   * inspector_signature

2. All critical chemical elements are within their maximum limits:
   * carbon_c_pct ≤ 0.080
   * phosphorus_p_pct ≤ 0.030
   * sulphur_s_pct ≤ 0.030

3. All critical mechanical properties are within their limits:
   * yield_strength_re_mpa ≥ 140 AND ≤ 210
   * tensile_strength_rm_mpa ≥ 270 AND ≤ 350

4. Exactly one non-critical property is out of tolerance and its deviation is ≤ 2%.
   Calculate deviation as follows:
   * manganese_mn_pct — max 0.400 — deviation = ((value − 0.400) / 0.400) × 100
   * titanium_ti_pct — max 0.300 — deviation = ((value − 0.300) / 0.300) × 100
   * elongation_a80_pct — min 38 — deviation = ((38 − value) / 38) × 100
   * plastic_strain_ratio_r90 — min 1.6 — deviation = ((1.6 − value) / 1.6) × 100
   * strain_hardening_exp_n90 — min 0.18 — deviation = ((0.18 − value) / 0.18) × 100
   * hardness_hrb — max 60 — deviation = ((value − 60) / 60) × 100

5. No mandatory or critical field is null. A null value on any critical field
   cannot satisfy a CONDITIONAL.

Output only the following JSON. Do not add any text outside it:
{
  "verdict": "CONDITIONAL" | "NOT_CONDITIONAL",
  "rationale": "plain English summary of why this verdict was reached",
  "non_critical_deviation": {
    "field": null,
    "reported_value": null,
    "limit": null,
    "deviation_pct": null
  },
  "failed_checks": []
}

If verdict is CONDITIONAL:
- non_critical_deviation must be populated with the single offending field
- failed_checks must be empty

If verdict is NOT_CONDITIONAL:
- non_critical_deviation must be null
- failed_checks must list every check that failed using this format:
  {
    "field": "field_name",
    "reason": "plain English explanation",
    "reported_value": null,
    "limit": "the spec limit that was not met",
    "deviation_pct": null,
    "is_critical": false
  }